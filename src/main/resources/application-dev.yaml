spring:
  datasource:
    url: jdbc:h2:file:./data/propietariodb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      path: /h2-console
      enabled: true
      settings:
        web-allow-others: true
  #  security:
  #    user:
  #      name: admin
  #      password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always

#  data:
#    redis:
#      host: localhost
#      port: 6379