server:
  port: 0

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  sql:
    init:
      mode: never

DEV_PORT: 8081
RSA_PRIVATE: "tu_llave_privada_de_test_aqui"
RSA_PUBLIC: "tu_llave_publica_de_test_aqui"

spring.data.redis.host: localhost